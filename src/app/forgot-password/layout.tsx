// import '../styles/globals.css';
import '../../styles/globals.css';
export default function ForgotPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
      <section>
          { children}
    </section>
  );
}